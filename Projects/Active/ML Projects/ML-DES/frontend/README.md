# Frontend (static prototype)

Open `index.html` in a browser. The page queries the backend `/api/ping` endpoint when served from a simple static server.

> **Knowledge path:** [[FDG Ecosystem|FDG Ecosystem]] → [[Projects/Projects_Master_Index|Projects Master Index]] → this document
